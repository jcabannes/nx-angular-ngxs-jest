import { NgModule } from '@angular/core';

import { AppComponent, AppModule } from '@features';

@NgModule({
  imports: [AppModule],
  providers: [],
  bootstrap: [AppComponent],
})
export class MyAppModule {}
